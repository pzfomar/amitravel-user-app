import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.amitravel.usuario',
  appName: 'AmiTravel-Usuario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
